export type ClassroomRequestType = {
  name: string;
  description: string;
  capacity: number;
  active: boolean;
};

export type AulaResponseDTO = {
  name: string;
  description: string;
  capacity: number;
  active: boolean;
};
